import { theme } from 'antd';

// Palette A "Forest + Lime" — matches app/globals.css. Keep the two in step.
export const deskTheme = {
  algorithm: theme.darkAlgorithm,
  token: {
    colorPrimary: '#C8E832',
    colorBgBase: '#0A0F0D',
    colorTextBase: '#E8F0EC',
    colorBorder: '#2A5244',
    colorBorderSecondary: '#1E3A30',
    borderRadius: 3,
    fontFamily: "'Inter', -apple-system, 'Segoe UI', Helvetica, Arial, sans-serif",
    fontSize: 15,
    wireframe: false,
  },
};
